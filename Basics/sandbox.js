var area = function (length) { return length * length; };
console.log(area(5));
var cArea = function (radius) { return Math.PI * (Math.pow(radius, 2)); };
var sArea = function (l, b, h) { return l * b * h; };
